/**
 * Separate entry point for React Hook Form integration.
 *
 * Import from "@charlietogolden/charlie-ui/form" to use Form + Field with RHF.
 * Requires react-hook-form as a peer dependency.
 */
export { Form, type FormProps, type FormFieldProps } from "./components/Form";
